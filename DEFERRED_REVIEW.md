# DEFERRED_REVIEW — รายการที่จดไว้รอตรวจหลังรอบ 2

รายการในไฟล์นี้ **ไม่ใช่** เรื่องวิกฤต (ไม่ใช่ security bug / policy bypass / ข้อมูลเสียหาย)
จึงตั้งใจไม่แก้ในรอบนี้ เพื่อไม่ให้รอบ 2 บวมเกินขอบเขต

---

## 🔴 ต้องทำก่อนเปิดใช้จริงกับลูกค้า (ไม่ใช่งานโค้ด แต่สำคัญที่สุด)

### D-1 ยืนยันกฎของ Meta กับเอกสารจริง ก่อนเปิด transport ตัวใดตัวหนึ่ง
ตอนเขียนรอบนี้ผมเข้าเอกสารของ Meta โดยตรงไม่ได้ (โดนจำกัดการเข้าถึง)
จึงตั้งค่าเริ่มต้นทุกช่องทางที่ต้องขออนุมัติไว้เป็น **ปิด + ยังไม่ยืนยัน**

ก่อนเปิด ต้องเช็คทีละข้อ แล้วค่อยตั้ง `..._VERIFIED=true` ใน `.env.local` :

| ช่องทาง | ต้องยืนยันอะไร |
|---|---|
| `HUMAN_AGENT` | ยื่น App Review ผ่านแล้วหรือยัง / กรอบเวลายังเป็น 7 วันไหม |
| `UTILITY` | Messenger มีช่องทางนี้จริงไหม / ชื่อ permission คืออะไร / รูปแบบ payload ที่ถูกต้อง |
| `MARKETING` | เงื่อนไขการคิดเงิน / วิธีเช็ค eligibility รายบุคคล |
| Instagram | มี `UTILITY` / `MARKETING` เทียบเท่า Messenger ไหม (ตอนนี้ถือว่า **ไม่มี**) |

> ⚠️ พบข้อมูลว่า Meta กำลังทยอยเลิกรองรับ message tag แบบเก่า
> (`POST_PURCHASE_UPDATE`, `ACCOUNT_UPDATE`, `CONFIRMED_EVENT_UPDATE`)
> ระบบนี้จึง **ไม่มี** ทางถอยไปใช้ tag เหล่านั้นเลย ตามที่ตกลงกันไว้
> ข้อมูลนี้ยังไม่ได้ยืนยันกับเอกสารทางการ — ต้องเช็คก่อนวางแผนการแจ้งเลขพัสดุ

### D-2 payload ของ UTILITY / MARKETING ยังเป็นโครงที่ยังไม่ได้ยืนยัน
`src/server/transports/utility.ts` และ `marketing.ts` ประกอบ payload ตามที่คาดว่าถูก
แต่ยังไม่ได้ทดสอบกับ Meta จริง — จึงตั้ง `verified=false` ไว้และ engine จะไม่เลือกใช้
**ต้องแก้ไฟล์ทั้งสองให้ตรงเอกสารจริงก่อนเปิดใช้**

---

## 🟡 งานที่รอรอบถัดไปตามลำดับในสเปก

### D-3 การเช็ค marketing eligibility รายบุคคล (ยังไม่มีตัวไปถาม Meta)
ตอนนี้ engine อ่านค่า `customers.marketing_eligible` / `marketing_checked_at` จากฐานข้อมูล
แต่ยังไม่มีงานที่ไปถาม Meta มาเติมค่าให้ → ในทางปฏิบัติ MARKETING จึงยังส่งไม่ได้เสมอ
ซึ่งถูกต้องและปลอดภัยแล้วสำหรับตอนนี้

### D-4 `messages.sent_with_human_agent_tag` ยังไม่มีใครเขียน
คอลัมน์นี้มีอยู่ตั้งแต่รอบ 1 แต่แถวในตาราง `messages` จะถูกสร้างในรอบหน้า (หน้าแชท)
ตอนบันทึกข้อความที่ส่งสำเร็จ ต้องเซ็ตค่านี้จาก `decision.transport === 'HUMAN_AGENT'`
(ข้อมูลพร้อมแล้วใน `message_sends.selected_transport`)

### D-5 webhook ยังไม่มี — การกันข้อความซ้ำฝั่งขาเข้ายังไม่ถูกใช้งาน
`messages.meta_message_id` มี unique index รออยู่แล้วตั้งแต่รอบ 1
และ `webhook_queue` ก็พร้อม แต่ตัวรับ webhook เป็นงานของรอบหน้า

### D-6 `/api/policy/preview` ส่งเนื้อหาหลอกเข้า engine (ยังเหมือนเดิม)
เพราะ `decide()` ต้องการเนื้อหาถึงจะตัดสินได้ หน้าตรวจสถานะจึงใส่ข้อความสั้น ๆ แทน
ไม่กระทบความถูกต้อง (ไม่มีอะไรถูกส่งออกไป) แต่ถ้าจะให้สะอาดกว่านี้
ควรเพิ่มโหมด `probe` ใน `decide()` ที่ข้ามการตรวจเนื้อหา

### D-7 ตัวล็อกกันแอดมินชน (`locked_by_admin_id`, `locked_at`)
คอลัมน์พร้อมแล้ว แต่ตรรกะปลดล็อกอัตโนมัติหลัง 3 นาทีเป็นงานของหน้าแชท

---

## 🟢 เรื่องเล็กน้อย

### D-8 ~~`recordAttempt()` รับ `metaMessageId` มาแต่ยังไม่ได้ใช้~~ ✅ แก้แล้วรอบ 2.1
เพิ่มคอลัมน์ `meta_message_id` ทั้งใน `send_attempts` และ `message_sends`

### D-9 เวลาที่ใช้คำนวณกรอบเป็นเวลานาฬิกาตรง ๆ
ไม่ได้คิดเรื่องเวลาออมแสง ซึ่งไม่กระทบไทย (UTC+7 ตลอดปี)
ถ้าวันหนึ่งขยายไปประเทศที่มี DST ต้องกลับมาดูจุดนี้

### D-10 ยังไม่มีหน้าจอดู `send_attempts` สำหรับแอดมิน
ตอนนี้ดูได้จาก Supabase โดยตรง ซึ่งพอสำหรับช่วงนี้
สเปกบอกว่าตารางนี้คือ "ที่แรกที่ดูเวลาส่งไม่ผ่าน" — ควรมีหน้าจอในรอบเก็บงาน


---

## 🆕 เพิ่มหลังตรวจรอบ 2.1

### D-11 ยังไม่มีตัวกวาดงานที่ค้างสถานะ "ไม่ทราบผล"
ตอนนี้ถ้าเจอ `outcome_unknown` ระบบจะหยุดและบันทึกไว้ ซึ่งถูกต้องแล้ว
แต่ยังไม่มีหน้าจอหรือการแจ้งเตือนให้คนรู้ว่ามีงานค้างอยู่
ดูได้จาก Supabase ตรง ๆ ก่อน :

```sql
select * from message_sends where status = 'outcome_unknown' order by created_at desc;
```

ควรทำหน้าจอ + แจ้งเตือน Telegram ในรอบเก็บงาน (สเปกสัปดาห์ 7)

### D-12 claim ที่ค้างค้างอยู่ ไม่มีตัวกวาดอัตโนมัติ
ถ้า process ตายกลางทาง แถวจะค้างที่ `claimed` จนกว่าจะมีคนเรียกกุญแจเดิมซ้ำ
แล้วค่อยถูกเปลี่ยนเป็น `outcome_unknown`
มี index `message_sends_stale_claim_idx` รออยู่แล้ว ควรมีงานกวาดเป็นระยะในรอบที่มี worker

### D-13 ตัวแปลง REST ในชุดทดสอบ ไม่ใช่ PostgREST ตัวจริง
`tests/pg/harness.ts` เขียนขึ้นเองให้พูดภาษาเดียวกับ Supabase เท่าที่โค้ดเราใช้
ครอบคลุมเส้นทางที่ใช้จริงทั้งหมด แต่ไม่ใช่ของจริง 100%
ถ้าวันหนึ่งใช้ฟีเจอร์ PostgREST ที่ซับซ้อนขึ้น (embed หลายชั้น / filter แปลก ๆ)
ต้องกลับมาดูจุดนี้ หรือเปลี่ยนไปรัน Supabase CLI ในเครื่องแทน

### D-14 การกันปลอมแหล่งที่มา ไม่ใช่ sandbox ความปลอดภัย
`isTrustedProvenance()` ใช้ WeakSet จำ "ตัวจริง" ไว้ ซึ่งกันการก๊อป/ปลอมได้จริง
แต่โค้ดที่อยู่ในโปรเซสเดียวกันและตั้งใจจะโกง ย่อมหาทางได้เสมอ
เป้าหมายคือทำให้ "เผลอทำผิดโดยไม่ตั้งใจ" เป็นไปไม่ได้ และให้ CI จับได้ทันที
ไม่ได้อ้างว่าเป็นการป้องกันระดับ sandbox

### D-15 ยังไม่รับประกัน exactly-once (และไม่ควรอ้างว่าทำได้)
Meta API ไม่ได้ให้การรับประกันนี้กับเรา ระบบจึงเลือกทางที่ปลอดภัยกว่าคือ
**ส่งอย่างมากหนึ่งครั้ง** เมื่อผลลัพธ์ไม่แน่นอน
แปลว่าในกรณีที่เน็ตขาดพอดี อาจมีข้อความที่ส่งถึงลูกค้าแล้วแต่ระบบไม่รู้
และจะไม่ส่งซ้ำให้ — ต้องมีคนไปเปิดแชทดู
นี่คือการแลกโดยตั้งใจ : ส่งขาดแก้ได้ ส่งซ้ำแก้ไม่ได้


### D-16 extension `pg_trgm` อยู่ใน schema `public` (ตั้งใจไม่ย้าย)
ตัวตรวจของ Supabase แนะนำให้ย้าย extension ออกจาก `public`
แต่ **ย้ายตอนนี้จะทำให้การค้นหาพัง** เพราะ index สองตัว
(`customers_name_trgm_idx`, `canned_title_trgm_idx`) ผูกกับ operator class
`gin_trgm_ops` ของ extension นี้ พอย้าย schema แล้ว index หา operator class ไม่เจอ

ความเสี่ยงของการปล่อยไว้ต่ำมาก — pg_trgm ไม่มีฟังก์ชันที่แก้ข้อมูลได้
ถ้าจะย้ายจริง ต้องทำพร้อมกันสามอย่างในธุรกรรมเดียว :
drop index → alter extension set schema → create index ใหม่
เก็บไว้ทำตอนรอบเก็บงาน ไม่ใช่ตอนนี้

---

## 🆕 เพิ่มหลังรอบ 3A (ทางเข้าข้อมูล)

### D-17 🔴 รูปภาพยังไม่ได้ดาวน์โหลดเก็บ — ลิงก์จาก Meta หมดอายุได้
สเปกหัวข้อ 6.2 บอกชัดว่าต้องดาวน์โหลดรูปเก็บทันทีที่ webhook เข้า
เพราะลิงก์ที่ Meta ส่งมามี token หมดอายุติดอยู่ด้วยเหตุผลด้านความเป็นส่วนตัว

รอบนี้ยังไม่มี Cloudflare R2 จึงเก็บได้แค่ลิงก์ไว้ใน `messages.attachments`
**แปลว่าถ้าปล่อยไว้นาน รูปจะเปิดไม่ได้ โดยเฉพาะสลิปโอนเงิน**

ตอนนี้ยังไม่กระทบเพราะยังไม่ได้เปิดใช้กับลูกค้าจริง
แต่ **ต้องทำก่อนเปิดใช้จริงเด็ดขาด** — ตั้ง R2 แล้วให้ตัวประมวลผลคิว
ดาวน์โหลดไฟล์ขึ้น R2 ทันทีก่อนบันทึกแถวข้อความ

### D-18 ที่มาของแชทใช้กติกา "ของใหม่ทับของเก่า"
ถ้าลูกค้าคนเดิมคลิกแอดใหม่แล้วทักมาอีก ระบบจะทับ `referral_ad_id` ด้วยแอดล่าสุด
(ถ้ารอบนั้นไม่มีข้อมูลที่มา จะเก็บของเดิมไว้ ไม่ลบทิ้ง)

เลือกแบบนี้เพราะ Dashboard ต้องตอบว่า "ยอดนี้มาจากแอดไหน" ของรอบล่าสุด
แต่ยังไม่ได้ยืนยันกับเจ้าของร้านว่าตรงกับที่ต้องการวัดผลจริงไหม
ถ้าอยากได้แบบ "จำแอดแรกที่พามา" ต้องแก้ที่ `ingest_inbound_message` ที่เดียว

### D-19 ยังไม่มีตัวกวาดคิวอัตโนมัติบนเซิร์ฟเวอร์จริง
ตอนนี้คิวถูกประมวลผลทันทีหลังตอบ webhook ด้วย `after()` ของ Next.js
ซึ่งพอสำหรับการใช้งานปกติ และมี `npm run worker` เป็นตาข่ายกันพลาด

แต่ยังไม่ได้ตั้งให้ worker รันอัตโนมัติบนเซิร์ฟเวอร์จริง
ถ้าย้ายไปแพลตฟอร์มที่ตัดงานเบื้องหลังทิ้งหลังตอบคำขอ (serverless บางเจ้า)
ต้องตั้ง worker เป็น service แยกทันที ไม่งั้นข้อความจะค้างในคิว

### D-20 เหตุการณ์จาก webhook ที่ยังไม่รองรับ
ตัวแกะรู้จักและ "ตั้งใจข้าม" ของพวกนี้ไว้แล้ว พร้อมจดเหตุผล :
`read` (ลูกค้าอ่านแล้ว) · `delivery` (ส่งถึงแล้ว) · `reaction` (กดใจ) ·
`postback` (กดปุ่ม → รอบคีย์เวิร์ด) · `changes` (คอมเมนต์ → รอบคอมเมนต์) ·
`standby` (แอปอื่นถือสิทธิ์ตอบ)

ไม่ใช่ปัญหา — แค่ยังไม่ถึงรอบของมัน

---

## 🆕 เพิ่มหลังรอบ 3B (หน้าแชท)

### D-21 ยังไม่ได้ใช้ Realtime ของ Supabase — ใช้การดึงซ้ำเป็นระยะแทน
สเปกหัวข้อ 5.1 เขียนว่า "Realtime" ไว้ แต่รอบนี้ใช้การดึงซ้ำ (polling) แทน :
ลิสต์แชททุก 8 วินาที / ข้อความในห้องที่เปิดอยู่ทุก 4 วินาที

**เหตุผลที่ยังไม่ใช้ Realtime** : Realtime ของ Supabase ต้องให้เบราว์เซอร์
ต่อตรงเข้าฐานข้อมูลด้วย anon key แต่ระบบเราตั้งใจปิด RLS ตายไว้
(เปิด RLS แต่ไม่มี policy) เพื่อให้ทุกการเข้าถึงผ่านเซิร์ฟเวอร์ของเราเท่านั้น
ถ้าจะใช้ Realtime ต้องเขียน RLS policy ครบทุกตาราง + ออก JWT ให้แอดมินแต่ละคน
ซึ่งเป็นการเปลี่ยนสถาปัตยกรรมความปลอดภัยทั้งระบบ — ใหญ่เกินกว่าจะรวบในรอบนี้

**พอไหมสำหรับตอนนี้** : แอดมิน 3-5 คน ดึงทุก 4-8 วินาที = ไม่กี่ร้อยคำขอต่อชั่วโมง
ยังห่างจากขีดจำกัดของ Supabase free tier มาก
ถ้าวันหนึ่งแอดมินเยอะขึ้นหรืออยากได้ทันทีจริง ๆ ค่อยกลับมาทำ

### D-22 การส่งรูปในห้องแชทยังไม่เปิด
ช่องพิมพ์ยังไม่มีปุ่มแนบรูป เพราะต้องมี R2 ก่อน (D-17)
โครงฝั่งหลังบ้านรองรับแล้ว (`SendContent.images` + `attachment_id`)
เหลือแค่ที่เก็บไฟล์กับหน้าจอ

### D-23 ค้นหายังครอบไม่ครบตามสเปก
สเปก 5.1 บอกว่าค้นได้ด้วย ชื่อ / เบอร์ / เลขออเดอร์ / เลขพัสดุ
รอบนี้ทำได้แค่ **ชื่อกับเบอร์** เพราะตารางออเดอร์กับเลขพัสดุยังไม่มีข้อมูลจริง
(มาในรอบ 5) เมื่อถึงรอบนั้นให้เพิ่มใน `listConversations()` ที่เดียว

### D-24 นาฬิกา 24 ชม. ในลิสต์แชทเป็นตัวเลขฝั่งหน้าเว็บ
ตัวเลข "เหลือ X ชม." ที่โชว์ในลิสต์คำนวณจากหน้าเว็บเพื่อความไว
และ **ไม่ใช่** ตัวตัดสินว่าส่งได้จริงไหม — ตัวตัดสินคือ Policy Engine เสมอ
ซึ่งหัวห้องแชทเรียกมาแสดงอีกที (`/api/policy/preview`)

ถ้าวันหนึ่ง Meta เปลี่ยนกรอบเวลา ตัวเลขในลิสต์จะคลาดเคลื่อนชั่วคราว
แต่ระบบจะไม่ส่งผิดกฎ เพราะการตัดสินจริงไม่ได้อยู่ที่นี่
(ถ้าอยากให้ตรงเป๊ะ ต้องให้ API ส่งกรอบเวลามาด้วย — เก็บไว้ทำตอนรอบเก็บงาน)

### D-25 ยังไม่มีเมนูแตะข้อความ / ปัดขวายกมาอ้างอิง
เป็นงานของรอบ 4 ตามลำดับในสเปก (สัปดาห์ 3 — เครื่องมือแอดมิน)

---

## 🆕 เพิ่มหลังรอบ 4 (เครื่องมือแอดมิน)

### D-26 "ยกมาอ้างอิง" เป็นการวางข้อความนำหน้า ไม่ใช่ reply จริงของ Meta
กด "ยกมาอ้างอิง" หรือปัดขวา จะได้ข้อความเดิมนำหน้าด้วย `>` ในช่องพิมพ์
ซึ่งลูกค้าจะเห็นเป็นข้อความธรรมดา ไม่ใช่ฟีเจอร์ "ตอบกลับข้อความนี้" ของ Messenger

ทำแบบนี้เพราะการ reply จริงต้องส่ง `reply_to.mid` ไปกับ payload
ซึ่งเป็นการแตะโครงสร้าง payload ของ transport — ต้องยืนยันกับเอกสาร Meta ก่อน (D-1)
และต้องแก้ที่ adapter ไม่ใช่ที่หน้าเว็บ

### D-27 ชุดคำตอบยังไม่รองรับรูปภาพ
ตาราง `canned_responses.images` มีอยู่แล้วตั้งแต่รอบ 1 และสเปก 6.2 บอกให้ใช้
reusable `attachment_id` แต่ต้องมี R2 ก่อน (D-17) รอบนี้จึงทำเฉพาะข้อความ

### D-28 การค้นชุดคำตอบยิง API ทุกครั้งที่พิมพ์ (หน่วง 150 มิลลิวินาที)
พอสำหรับแอดมิน 3-5 คน แต่ถ้าชุดคำตอบเยอะขึ้นมาก ๆ
ควรโหลดมาเก็บไว้ในหน้าเว็บทั้งก้อนแล้วค้นในเครื่องแทน

### D-29 แท็กอัตโนมัติ (`tags.is_auto`) ยังไม่มีใครใส่
คอลัมน์มีอยู่ตั้งแต่รอบ 1 และหน้าจอกันไม่ให้ลบแท็กอัตโนมัติแล้ว
แต่ยังไม่มีตรรกะที่ใส่แท็กให้เอง (เช่น "รอโอน" ตอนสร้างออเดอร์)
เป็นงานของรอบออเดอร์

### D-30 ตัวดึงที่อยู่ยังไม่รู้จักเบอร์บ้าน
ตั้งใจรับเฉพาะเบอร์มือถือ (06/08/09) เพราะเบอร์บ้าน 9 หลักสับสนกับเลขอื่นง่าย
ถ้าวันหนึ่งเจอลูกค้าให้เบอร์บ้านบ่อย ค่อยกลับมาเพิ่ม พร้อมเพิ่มชุดทดสอบกันสับสน

---

## 🆕 บทเรียนจากการใช้งานจริง (หลังรอบ 4)

### D-31 ✅ แก้แล้ว — ปุ่ม "ทดสอบการเชื่อมต่อ" เคยรายงานผิด
เดิมตัวทดสอบถาม `GET /{page_id}?fields=id,name` ซึ่ง Meta บังคับว่าต้องมี
สิทธิ์ `pages_read_engagement` หรือฟีเจอร์ "Page Public Metadata Access"

ผลคือ **token ที่ส่งข้อความได้จริงกลับถูกรายงานว่าใช้ไม่ได้**
ทั้งที่ข้อความขาเข้าไหลเข้าระบบตามปกติอยู่แล้ว — เป็นการโกหกเจ้าของร้าน

เปลี่ยนไปถาม `GET /me` แทน (Page Access Token → "me" คือตัวเพจเอง)
ซึ่งไม่ต้องใช้สิทธิ์เพิ่มเติมใด ๆ นอกจากตัว token เอง
ส่วน Instagram ยังถามด้วย id ตรง ๆ เพราะ `/me` จะได้เพจ Facebook ไม่ใช่บัญชี IG

**บทเรียน** : ตัวทดสอบต้องใช้สิทธิ์ "ไม่เกินกว่า" สิ่งที่ระบบใช้งานจริง
ไม่งั้นมันจะกลายเป็นตัวสร้างความสับสนแทนที่จะเป็นตัวช่วย

### D-32 ข้อความ error จาก Meta ต้องแปลให้ทำตามได้
เพิ่มตัวแปล error code ที่เจอบ่อยเป็นคำแนะนำภาษาไทยที่บอกว่าต้องไปทำอะไรต่อ
(190 = token หมดอายุ / 100 + pages_read_engagement = ขาดสิทธิ์ / 200,10 = สิทธิ์เพจไม่พอ)

และจด "ข้อความดิบจาก Meta" ลงล็อกเซิร์ฟเวอร์เสมอ
เดิมจดแค่ข้อความไทยกลาง ๆ ซึ่งอ่านแล้วไล่ปัญหาต่อไม่ได้เลย

### D-33 ✅ แก้แล้ว (ก้อน BUGFIX-1) — ดึงชื่อลูกค้าไม่ได้
> **สาเหตุจริงไม่ใช่สิทธิ์ Meta อย่างเดียว — เป็นบั๊กฝั่งเราด้วย**
>
> `syncProfileIfNeeded()` จด `profile_synced_at` **ทุกครั้งแม้ดึงไม่สำเร็จ**
> แล้วรอบถัดไปเช็คว่า "เคยจดแล้วให้ข้าม" → **ยอมแพ้ถาวร**
> ต่อให้เจ้าของร้านแก้สิทธิ์ Meta ถูกแล้ว ลูกค้าเก่าทุกคนก็ค้างเป็น "ลูกค้า xxxxxx" ตลอดกาล
>
> เจตนาเดิม (ไม่อยากยิงถาม Meta ทุกข้อความ) ถูก แต่วิธีแก้เหมารวม
> "ล้มเหลว" ไปปนกับ "สำเร็จ"
>
> **แก้ด้วย** migration `0014` + `server/meta/profile-sync.ts` :
> - `profile_synced_at` จดเฉพาะตอนได้ข้อมูลจริง
> - ล้มเหลว → เว้นระยะแล้วลองใหม่ (1 นาที → 10 นาที → 1 ชม. → 6 ชม. → 24 ชม.)
> - เพดาน 8 ครั้ง แล้วมีปุ่ม "ดึงชื่อ" ในหัวห้องให้กดเองได้เสมอ
> - บันทึก**เหตุผล**ที่พลาดเป็นภาษาไทยที่บอกว่าต้องไปแก้อะไร (ชื่อสิทธิ์ที่ขาด)
> - แยก "ความเป็นส่วนตัวของลูกค้า" ออกจาก "สิทธิ์ token ไม่พอ" — เดิมปนกันจนไล่แก้ token ไม่จบ
> - migration ปลดล็อกแถวเดิมที่ถูกตีตราว่าดึงแล้วทั้งที่ไม่มีชื่อ
> - ห้ามเขียนทับชื่อจริงด้วยค่าว่าง
> - 19 PG tests + พิสูจน์แล้วว่าจับบั๊กเดิมได้จริง (ใส่บั๊กกลับ → แดง 6 ข้อ)
>
> **ยังต้องรอ owner** : ตรวจสิทธิ์ `pages_messaging` / `instagram_manage_messages`
> ตอนสร้าง token — ถ้าสิทธิ์ยังขาด ระบบจะบอกเหตุผลตรง ๆ แล้ว ไม่เงียบเหมือนเดิม

<details><summary>บันทึกเดิมของ D-33 (เก็บไว้เป็นประวัติ)</summary>


`[meta/profile] ดึงโปรไฟล์ไม่ได้ ... permanent` ยังเกิดอยู่
ลิสต์แชทจึงแสดงเป็น "ลูกค้า xxxxxx" แทนชื่อจริง

**ไม่กระทบการรับ-ส่งข้อความ** แต่ใช้งานจริงลำบาก
ตอนนี้จดข้อความดิบจาก Meta ไว้แล้ว (D-32) รอบหน้าจะรู้ว่าขาดสิทธิ์ตัวไหน
คาดว่าเป็นสิทธิ์กลุ่มเดียวกับ D-31 — ถ้าเพิ่มสิทธิ์ตอนสร้าง token แล้วน่าจะหายไปด้วย

</details>

### D-34 หน้าอินบ็อกซ์ยิง API ถี่เกินไปเมื่อเปิดทิ้งไว้นาน
จากล็อกจริง : ลิสต์แชททุก 8 วินาที + ข้อความทุก 4 วินาที + ต่ออายุล็อกทุก 45 วินาที
บางคำขอใช้เวลา 1-3 วินาที (Supabase อยู่สิงคโปร์ + โหมด dev)

ยังพอรับได้สำหรับแอดมิน 3-5 คน แต่ควรทำในรอบเก็บงาน :
- หยุดดึงเมื่อแท็บไม่ได้อยู่หน้าจอ (`document.visibilityState`)
- ดึงเฉพาะข้อความที่ใหม่กว่าอันล่าสุดที่มีอยู่ แทนการดึงทั้งห้องทุกรอบ

---

## 🆕 รอบ 5 — ออเดอร์ / สินค้า / โปรโมชัน

### D-35 ยังไม่มีที่อัปโหลดสลิปโอนเงิน
คอลัมน์ `orders.slip_url` มีอยู่ และหน้ารายละเอียดออเดอร์แก้ค่านี้ได้ผ่าน API แล้ว
แต่ยัง **ไม่มีปุ่มอัปโหลดจริง** เพราะที่เก็บไฟล์ยังไม่มี (ผูกกับ D-17 โดยตรง)

ทำไมยอมค้างไว้ : ถ้าทำปุ่มอัปโหลดตอนนี้ ไฟล์จะไปจบที่ URL ชั่วคราวของ Meta
ซึ่งหมดอายุ — สลิปเงินที่เปิดไม่ได้ย้อนหลังแย่กว่าไม่มีปุ่มเลย
**ต้องทำ D-17 (Cloudflare R2) ก่อนเสมอ**

### D-36 ยังไม่มีค่าส่งอัตโนมัติ / ตารางค่าส่ง
สเปกข้อ 4 พูดถึงค่าส่ง แต่รอบนี้ให้กรอกเองในกล่องสร้างออเดอร์
ยังไม่มีกฎแบบ "ซื้อครบ X ส่งฟรี" หรือค่าส่งตามเขต

ทำไมยอมค้างไว้ : ร้านยังไม่ได้บอกกฎค่าส่งจริง เดาไปก่อนแล้วต้องรื้อ
เมื่อมีกฎแน่นอน ให้ใส่ใน `calculateOrder()` ที่เดียว — ห้ามแทรกที่หน้าเว็บ

### D-37 หน้าออเดอร์ยังไม่มีสรุปยอดตามช่วงเวลา
ตอนนี้สรุปแค่ "รวมของรายการที่กรองอยู่" ยังไม่มีกรองตามวันที่ / ตามแอดมิน
(API รองรับ `admin_id` แล้ว แต่หน้าจอยังไม่มีตัวเลือก)
เป็นงานของรอบรายงาน (สเปก 5.4) ซึ่งจะทำ dashboard ทีเดียวพร้อมกัน

### D-38 `promotions.type = 'boxset'` ยังต้องกรอก `pick` เอง
บ็อกซ์เซ็ตแปลว่า "ครบทุกสี" ซึ่งจำนวนควรมาจากจำนวนสินค้าที่เปิดใช้อยู่
แต่ตัวคิดราคาเป็นฟังก์ชันบริสุทธิ์ (ห้ามต่อฐานข้อมูล) จึงนับเองไม่ได้
ตอนนี้ให้เจ้าของร้านกรอกจำนวนใน `config.pick`

ถ้าจะทำให้อัตโนมัติ ต้องนับที่ชั้น API แล้วส่ง `pick` เข้ามา
**ห้าม** แก้โดยให้ `calculateOrder()` ต่อฐานข้อมูล — นั่นจะทำให้ทดสอบราคาไม่ได้อีกเลย

### D-39 แท็กอัตโนมัติตอนสร้างออเดอร์ (ต่อจาก D-29)
ยังไม่ได้ใส่แท็ก "รอโอน" / "ปิดการขายแล้ว" ให้เองตอนสร้างหรือเปลี่ยนสถานะออเดอร์
รอให้ใช้งานจริงสักพักก่อนว่าแอดมินอยากได้แท็กชื่ออะไรบ้าง
ใส่เดาไปก่อนแล้วต้องมาลบทีหลัง จะเลอะกว่าเดิม

### D-40 ลบออเดอร์ไม่ได้ — ตั้งใจ
`/api/orders/[id]` มีแค่ GET กับ PATCH **ไม่มี DELETE**
ออเดอร์ที่ไม่เอาแล้วให้เปลี่ยนสถานะเป็น `cancelled` แทน

เหตุผล : เลขออเดอร์ถูกอ้างถึงข้างนอกระบบ (สลิป ใบส่งของ แชทกับลูกค้า)
ลบทิ้งแล้วตามกลับไม่ได้ และประวัติแก้ไขก็หายไปด้วย
สิทธิ์ `order.delete` ยังมีอยู่ในระบบสิทธิ์ เผื่อวันหนึ่งต้องมีจริง ๆ แต่ยังไม่มีเส้นทางไหนใช้

---

## 🆕 รอบ 6 — ตอบอัตโนมัติ / ส่งรูป / วิธีจัดส่ง

### D-41 🔴 SECURITY DEFERRED — หมุน secret ทั้งชุด (ตามที่เจ้าของสั่ง)
`META_VERIFY_TOKEN` เคยปรากฏใน log ของ dev server ที่วางมาในแชท

⚠️ **สาเหตุที่หลุด** : Next.js dev server พิมพ์ query string ของทุกคำขอลง terminal
และ Meta ส่ง `hub.verify_token` มาทาง query string — เราปิดการพิมพ์บรรทัดนั้นของ Next ไม่ได้
โค้ดของเราเองไม่เคย log ค่านี้ (ตรวจแล้ว log แค่ "ชื่อ" ตัวแปร ไม่ใช่ค่า)

**ทางแก้เวลาจะแปะ log** : ตัดบรรทัดที่มี `verify_token` ออกก่อน หรือใช้
`npm run dev 2>&1 | sed -E 's/(verify_token=)[^&"]*/\1REDACTED/g'`

เจ้าของสั่งไว้ว่าจะหมุน secret ทีเดียวช่วง final hardening — ไม่บล็อกรอบ 6
รายการที่ต้องหมุนตอนนั้น : `META_VERIFY_TOKEN`, `META_APP_SECRET`,
Page Access Token, `SUPABASE_SERVICE_ROLE_KEY`, `SESSION_SECRET`

### D-42 ยังไม่มี regex ในกฎคีย์เวิร์ด — ตั้งใจ
รองรับแค่ exact / contains / starts_with

**เหตุผล** : ถ้าเปิดให้แอดมินใส่ regex เอง จะเปิดช่อง ReDoS ทันที
pattern อย่าง `(a+)+$` ทำให้เซิร์ฟเวอร์ค้างได้ด้วยข้อความสั้น ๆ
และแอดมินร้านค้าไม่ควรต้องเข้าใจ regex เพื่อตั้งคำตอบอัตโนมัติ

**ถ้าจะเพิ่มจริง ต้องมีก่อน** : ตัวตรวจ pattern (ปฏิเสธ nested quantifier),
timeout ต่อการเทียบหนึ่งครั้ง, และเทสต์ที่ยิง pattern อันตรายเข้าไปแล้วต้องไม่ค้าง
**dependency** : ไม่มี — ทำได้ทุกเมื่อ / **แนะนำ** : เฟสท้าย ถ้ามีคนขอจริง

### D-43 ตอบอัตโนมัติยังส่งได้แต่ข้อความล้วน
`keyword_rules.reply_images` มีคอลัมน์อยู่ตั้งแต่ 0001 แต่ยังไม่ได้ใช้

**เหตุผลที่ยังไม่ทำ** : ต้องอัปโหลดรูปเก็บ `attachment_id` ไว้ล่วงหน้าตอนตั้งกฎ
และ `attachment_id` ของ Meta ผูกกับเพจ — กฎที่ใช้หลายเพจต้องมีหลาย id
ซึ่งซับซ้อนพอที่จะทำผิดได้ง่าย และไม่ใช่สิ่งที่ร้านต้องใช้ตั้งแต่วันแรก
**dependency** : ไม่มี (Attachment Upload API ทำเสร็จแล้วในรอบนี้) / **แนะนำ** : รอบ 8-9

### D-44 ส่งรูปได้ทีละรูป และส่งรูปพร้อมข้อความในครั้งเดียวไม่ได้
Meta ต้องการให้ข้อความกับรูปเป็นคนละ message อยู่แล้ว
ตอนนี้จึงเป็น "ส่งรูป" กับ "ส่งข้อความ" แยกปุ่มกัน ซึ่งตรงกับความจริงของ API
**แนะนำ** : ถ้าจะทำ caption ให้ส่งสองข้อความติดกัน แต่ต้องระวังเรื่องกันซ้ำให้ดี

### D-45 รูปที่แอดมินส่งไม่ได้เก็บสำเนาไว้ที่เรา (ต่อจาก D-17)
ใช้ Attachment Upload API ทำให้ไฟล์ไปอยู่ที่ Meta เราเก็บแค่ `attachment_id`

ผลคือ : ส่งรูปได้จริงตั้งแต่วันนี้ ✅ แต่ทำรายงาน/ย้ายระบบทีหลังยังต้องมี R2 อยู่ดี
**dependency** : D-17 / **แนะนำ** : ทำพร้อม D-17

### D-46 รูปขาเข้ายังใช้ลิงก์ชั่วคราวของ Meta
หน้าแชทแสดงรูปที่ลูกค้าส่งมาแล้ว และถ้าลิงก์หมดอายุจะขึ้นข้อความบอกตรง ๆ
แทนที่จะเป็นกรอบว่าง ๆ — แต่นี่เป็นการ "บอกความจริง" ไม่ใช่การแก้ปัญหา

🔴 **สลิปโอนเงินคือเหตุผลที่ D-17 ต้องทำก่อนใช้จริง** ไม่ใช่เรื่องความสวยงาม

### D-47 ยังไม่มีค่าธรรมเนียม COD แยกต่างหาก
`shipping_methods` มี `fee` ตัวเดียว ยังไม่แยก "ค่าส่ง" กับ "ค่าบริการเก็บเงินปลายทาง"

**เหตุผล** : ร้านยังไม่ได้บอกว่าคิดแยกไหม และคิดเท่าไหร่ — เดาไปก่อนแล้วต้องรื้อ
เมื่อรู้กฎจริง ให้เพิ่มคอลัมน์ `cod_fee` แล้วบวกใน `calculateOrder()` ที่เดียว
**ห้าม** แทรกที่หน้าเว็บ / **แนะนำ** : เมื่อมีข้อมูลจริง

### D-48 หน้าออเดอร์ยังไม่มีตัวกรองตามวิธีจัดส่ง/แอดมิน บนหน้าจอ
API รองรับ `shipping_method_id` และ `admin_id` แล้ว แต่หน้าจอมีแค่ สถานะ/การจ่ายเงิน/เพจ

**เหตุผล** : ใส่ตัวกรองครบทุกตัวทำให้แถบกรองยาวจนใช้บนมือถือลำบาก
รอดูว่าแอดมินใช้ตัวไหนจริงก่อน / **แนะนำ** : รอบ 8 พร้อมหน้ารายงาน

### D-49 การกันกดซ้ำยังพึ่ง idempotency key ต่อห้องแชท
ปุ่มส่งมีสถานะ disabled ระหว่างส่งอยู่แล้ว และ `sendMessage()` มีชั้นกันซ้ำที่ฐานข้อมูล
แต่ยังไม่ได้ทำ optimistic update (แสดงข้อความทันทีก่อนเซิร์ฟเวอร์ตอบ)

**เหตุผลที่ยังไม่ทำ** : ข้อความที่โผล่แล้วหายเพราะส่งไม่สำเร็จ สร้างความสับสนมากกว่ารอ 1 วินาที
โดยเฉพาะกับระบบที่ Policy Engine อาจปฏิเสธการส่ง / **แนะนำ** : เฟสท้าย ถ้าแอดมินบ่นว่าช้า

### D-50 ตัวจำลอง PostgREST ในชุดทดสอบเคยไม่รู้จัก `is.null`
🔴 **บทเรียนสำคัญ** : ตัวกรอง `.is('archived_at', null)` ถูกส่งไปแล้วตัวจำลองไม่รู้จัก
จึง **ข้ามเงื่อนไขไปเงียบ ๆ** ทำให้เทสต์ผ่านทั้งที่ของจริงยังไม่ถูกกรอง

จับได้เพราะชุดทดสอบวิธีจัดส่งเช็ก "เก็บเข้ากรุแล้วต้องหายจากลิสต์" ตรง ๆ
แก้ที่ `tests/pg/harness.ts` แล้ว (รองรับ `is.null` / `is.not.null` / `is.true` / `is.false`)

**สิ่งที่ต้องระวังต่อไป** : ตัวจำลองรู้จักเท่าที่เราสอน — ถ้าใช้ตัวกรองแบบใหม่ของ
PostgREST ต้องเพิ่มในตัวจำลองด้วย ไม่งั้นเทสต์จะ "ผ่านแบบโกหก"

### D-51 ยังไม่มี Ice Breakers / จับคู่ชื่อแอด
สเปกสัปดาห์ 4 มีสองข้อนี้อยู่ด้วย แต่ไม่ได้ทำในรอบนี้

**เหตุผล** : `messaging_referrals` (ที่มาจากแอด) ทำเสร็จตั้งแต่รอบ 3 แล้ว —
ที่ยังขาดคือ "แปลง ad_id เป็นชื่อแอดที่อ่านรู้เรื่อง" ซึ่งต้องต่อ Marketing API
เป็นคนละสิทธิ์และคนละชุด permission กับที่ใช้อยู่ ควรทำพร้อมหน้ารายงาน
**dependency** : Marketing API permission / **แนะนำ** : รอบ 8 พร้อม Dashboard

### D-52 Ice Breakers (คำถามยอดฮิตใต้ปุ่มเริ่มแชท)
ตั้งผ่าน Messenger Profile API ได้ แต่เป็นการตั้งค่า "ที่ตัวเพจ" ไม่ใช่ในระบบเรา
ต้องมีหน้าจัดการแยก + ต้องระวังว่าเขียนทับของที่ตั้งไว้ใน Business Suite
**แนะนำ** : รอบ 9 (เก็บงาน) พร้อมหน้าตั้งค่าเพจ

---

## 🆕 D-17 ปิดแล้ว ✅ (รอบ 6.5)

### D-17 ✅ แก้แล้ว — เก็บรูป/สลิปไว้เองอย่างถาวร
ดาวน์โหลดไฟล์แนบมาเก็บที่ Cloudflare R2 ทันทีที่รับ webhook ก่อนลิงก์ Meta หมดอายุ

**สิ่งที่ยังต้องทำเอง** : เปิดบัญชี R2 + ใส่ค่า 4 ตัวใน `.env.local` (ดู `docs/STORAGE.md`)
ระหว่างที่ยังไม่ตั้งค่า ระบบทำงานได้ปกติ แต่จดไฟล์เป็น `skipped`
**และทุกวันที่ยังไม่ตั้ง = สลิปที่หายแบบกู้คืนไม่ได้**

### D-53 🔴 บทเรียน — ไลบรารีที่ "สวมของปลอมไม่ได้" ทำให้เทสต์โกหก
`aws4fetch` ยิงด้วย `fetch` ตัวโลกเสมอ ใส่ตัวยิงของเราเข้าไปไม่ได้
ตอนแรกเขียน `new AwsClient({ fetch: myFetch })` ซึ่ง **ไลบรารีเมินเงียบ ๆ**
ผลคือชุดทดสอบวิ่งออกเน็ตจริง แล้วล้มเพราะ DNS ไม่ใช่เพราะโค้ดผิด

แก้โดยแยก **"เซ็น"** (`client.sign()`) ออกจาก **"ยิง"** (fetcher ของเรา)
**บทเรียน** : ถ้าไลบรารีไม่เปิดให้สวมของปลอม ให้ใช้เฉพาะส่วนที่บริสุทธิ์ของมัน
แล้วคุมส่วนที่ต่อโลกภายนอกเอง

### D-54 🔴 บทเรียน — อย่าเดาสาเหตุจากข้อความ error
เดิมแยก "ลิงก์หมดอายุ" ด้วย regex `/\((403|404|410)\)/` บนข้อความ error
พอถัง R2 ตอบ 403 (ตั้งค่าผิด) ระบบไปจดว่า **"ไฟล์ของลูกค้าหายถาวร"**
ซึ่งจะทำให้เจ้าของร้านเข้าใจผิดว่าสลิปหาย แล้วไปตามหาผิดที่

แก้เป็น `SourceGoneError` ที่โยนเฉพาะตอนดาวน์โหลดต้นทางเท่านั้น
**บทเรียน** : แยกสาเหตุด้วย "ชนิดของข้อผิดพลาด" ไม่ใช่การอ่านข้อความ

### D-55 🔴 บทเรียน — เทสต์ที่วูบวาบ (flaky) ซ่อนอยู่ตั้งแต่รอบ 6
`messaging.pg.test.ts > ส่งสำเร็จ → ล้างสถานะ "เคยถูกปฏิเสธ"` ล้มแบบสุ่ม ~2 ใน 5

**สาเหตุ** : เทสต์ตั้ง `window_closed_observed_at = now()` และ
`last_customer_message_at = now()` ในสองคำสั่งติดกัน
แต่ engine เทียบด้วย `>=` โดยตั้งใจ (fail closed — เชื่อ Meta ไว้ก่อน)
ถ้าสองเวลาตกในมิลลิวินาทีเดียวกัน ระบบจะบล็อกการส่ง

**ตัว engine ถูกแล้ว — เทสต์ต่างหากที่เขียนเจตนาไม่ชัดพอ**
แก้โดยให้เวลาที่สังเกตเก่ากว่าอย่างชัดเจน (`now() - interval '5 minutes'`)
ยืนยันด้วยการรันซ้ำ 8 รอบ + ชุดเต็ม 4 รอบ ผ่านหมด

⚠️ **รายงานรอบ 6 ที่บอกว่า "97 ผ่าน" จึงเป็นความจริงของ "รอบนั้น" เท่านั้น**
ไม่ใช่การรับประกันว่าเสถียร — จากนี้ไปเทสต์ที่แตะเรื่องเวลาต้องรันซ้ำหลายรอบก่อนเชื่อ

### D-56 รูปที่แอดมินส่งออกยังไม่เก็บสำเนา (ต่อจาก D-45)
ขาออกใช้ Attachment Upload API ของ Meta ไฟล์จึงอยู่ที่ Meta
ตอนนี้เก็บเฉพาะขาเข้ากับสลิป ซึ่งเป็นสองอย่างที่มีผลทางการเงิน
**แนะนำ** : เก็บขาออกด้วยเมื่อทำรายงาน/ย้ายระบบ (รอบ 8-9)

### D-57 ยังไม่มีตัวลองเก็บซ้ำสำหรับไฟล์ที่ `failed`
ไฟล์ที่ล้มเพราะเน็ตสะดุด (5xx) ยังลองใหม่ได้ในทางทฤษฎี แต่ยังไม่มีตัวไล่เก็บ
**เหตุผลที่ยังไม่ทำ** : ลิงก์ของ Meta หมดอายุเร็ว กว่าจะถึงรอบลองใหม่มักหมดแล้ว
คุ้มกว่าถ้าไปทำให้รอบแรกสำเร็จให้มากที่สุด
**แนะนำ** : ทำพร้อม worker ตัวจริง (รอบ 9) และตั้งเวลาให้ถี่พอ

### D-58 การดึงแชทเก่ายังต้องกดเอง ยังไม่มีตัวดึงอัตโนมัติเบื้องหลัง
รอบ 7 ทำเป็นปุ่มในหน้าจัดการเพจ กดหนึ่งครั้งดึงได้สูงสุด ~5,000 ห้อง
**เหตุผลที่ยังไม่ทำเป็นงานเบื้องหลัง** : Meta จำกัดจำนวนเรียกต่อชั่วโมงต่อเพจ
ถ้ายิงอัตโนมัติแล้วชนโควตา **ข้อความขาเข้าจริงจะเข้าไม่ได้ด้วย** ซึ่งเสียหายกว่ามาก
การให้คนกดเองทำให้เห็นผลทันทีและหยุดได้ทันทีถ้าผิดปกติ
**แนะนำ** : ทำเป็นคิวเบื้องหลังพร้อม worker ตัวจริง (รอบ 9) และเว้นจังหวะตามโควตา

### D-59 Conversations API ไม่มี "ที่มาของแชท" (มาจากแอดไหน)
ข้อความที่ดึงย้อนหลังจึงไม่มี referral (ADS / POST / SHORTLINK)
ตัวดึงส่ง `null` ทุกช่อง และฟังก์ชันบันทึกใช้ `coalesce` จึงไม่ไปลบของเดิมที่ webhook เคยได้มา
**ผลกระทบ** : แชทเก่าจะไม่มีป้าย "จากแอด" ให้ดู — เป็นข้อจำกัดของ Meta ไม่ใช่ของเรา
**แนะนำ** : ถ้าจำเป็นต้องใช้จริง ต้องดึงจาก Ads Insights แยก (นอกขอบเขตเฟสนี้)

### D-60 ไฟล์แนบของแชทเก่ายังไม่ถูกเก็บสำเนา
ตัวเก็บไฟล์ถาวร (D-17) ผูกอยู่กับสายรับข้อความขาเข้าเท่านั้น
การดึงย้อนหลังจึงบันทึกแค่ลิงก์ของ Meta ซึ่งเป็นลิงก์ชั่วคราว
**เหตุผลที่ยังไม่ทำ** : แชทเก่าหลายพันข้อความ = ดาวน์โหลดหลาย GB ในคำขอเดียว
จะ timeout และชนโควตาแน่นอน ต้องแยกเป็นคิวเบื้องหลังเท่านั้น
**แนะนำ** : ทำพร้อม worker (รอบ 9) โดยไล่เก็บจาก `media_assets` ที่ยังไม่มีสำเนา

### D-61 ⚠️ ขอบเวลาซ้ำ — เลือก `lte` แลกกับได้ของซ้ำที่ขอบ
ทั้งการไล่ข้อความเก่าและการไล่ลิสต์แชทใช้ `<=` ไม่ใช่ `<`
เพราะแชทที่ดึงย้อนหลังมาจาก Meta หลายรายการมีเวลาเท่ากันเป๊ะได้
ถ้าใช้ `<` รายการที่ตรงขอบพอดีจะ **หายไปเงียบ ๆ ทั้งกลุ่ม** ซึ่งอันตรายกว่ามาก
ผลข้างเคียงคือได้ของซ้ำหนึ่งชุดที่ขอบ — หน้าเว็บกรองทิ้งด้วย id เอง
และถ้ารอบไหนไม่ได้ของใหม่เลย จะถือว่า "หมดแล้ว" เพื่อกันการกดวนไม่รู้จบ
เพิ่มตัวตัดสินลำดับตอนเวลาเท่ากันแล้ว (`order by created_at desc, id desc`
และลิสต์แชทใช้ `last_message_at desc, id desc`) ลำดับจึงคงที่ทุกครั้งที่ถาม
— ก่อนหน้านี้ลำดับไม่คงที่ ซึ่งทำให้ "ข้ามรายการหายไปเงียบ ๆ" ได้จริง

⚠️ ตัวจำลอง PostgREST ในชุดทดสอบเดิมอ่านคอลัมน์เรียงแค่คอลัมน์แรก
ตัวตัดสินที่เพิ่งใส่จะกลายเป็นของหลอกทันที — แก้ที่ `tests/pg/harness.ts` แล้ว
(บทเรียนซ้ำรอย D-50 : ตัวจำลองที่เงียบเกินไปทำให้เทสต์ผ่านทั้งที่ของจริงยังไม่ถูก)

**ข้อจำกัดที่เหลือ** : ถ้ารายการที่เวลาเท่ากันเป๊ะมีมากกว่า "หนึ่งชุด"
(100 ข้อความในวินาทีเดียวกัน / 50 ห้องที่มีเวลาข้อความล่าสุดตรงกันเป๊ะ)
จะไล่ย้อนต่อไม่ได้และจะหยุดเอง (ตัดจบ ไม่วนไม่รู้จบ)
ในทางปฏิบัติแทบเป็นไปไม่ได้ จึงยังไม่แก้
**แนะนำ** : ถ้าเจอจริง ให้เปลี่ยนเป็น keyset เต็มรูป `(created_at, id)`
ซึ่งต้องเพิ่มความสามารถ `and(...)` ซ้อนใน `or=` ของตัวจำลองด้วย

### D-62 🔴 บทเรียน — encodeURIComponent กินเครื่องหมาย / ของเส้นทาง Meta
ตัวยิง Meta เข้ารหัส path **ทั้งเส้น** ด้วย `encodeURIComponent()`
ที่ผ่านมาไม่มีใครเรียกเส้นทางที่มี `/` (มีแต่ psid เดี่ยว ๆ กับ `me`) จึงไม่เคยพัง
พอรอบ 7 เรียก `<page-id>/conversations` มันกลายเป็น `<page-id>%2Fconversations`

**อาการที่เจ้าของร้านจะเจอ** : กดดึงแชทเก่าแล้ว Meta ตอบ error 100
ระบบแปลว่า "token ยังไม่มีสิทธิ์" → ไล่สร้าง token ใหม่ไม่จบ ทั้งที่ token ถูกอยู่แล้ว
เป็นบั๊กที่ "ข้อความแนะนำที่ดี" กลับพาไปผิดทาง

แก้เป็นเข้ารหัสทีละท่อน แล้วเพิ่มข้อทดสอบที่ตรวจหน้าตา URL จริง
(พิสูจน์แล้วว่าถ้าย้อนโค้ดกลับ เทสต์ข้อนั้นล้มทันที)
**บทเรียน** : ชุดทดสอบที่สวม fetch ปลอมต้อง "ตรวจ URL ที่ยิงออกไปด้วย"
ไม่ใช่ตรวจแค่ผลลัพธ์ ไม่งั้นของปลอมจะตอบถูกให้กับคำขอที่ผิด

### D-63 การรวมรายการยังไม่มองเห็นการแก้ไขเนื้อหาเดิม
`mergeByTime()` ตัดสินด้วย id + เวลา ถ้าเนื้อหาของรายการเดิมเปลี่ยน
โดยเวลาไม่เปลี่ยน ก็ยังอัปเดตให้ถูกอยู่ (เพราะเอาตัวใหม่มาแทนเสมอ)
แต่ผลข้างเคียงคือคืนอาเรย์ก้อนใหม่ทุกครั้งที่ดึงข้อมูล → React วาดใหม่ทั้งห้องทุก 4 วินาที
**ยังไม่แก้เพราะ** : พฤติกรรมนี้มีมาตั้งแต่รอบ 3 และยังไม่มีอาการช้าให้เห็น
ตัวที่เคยพลาดจริงคือป้าย "มีข้อความใหม่" เด้งมั่ว ซึ่งแก้แล้วด้วยการเทียบ id ของข้อความล่างสุด
**แนะนำ** : ถ้าห้องยาว ๆ เริ่มหนืด ให้ห่อ `MessageBubble` ด้วย `memo()` ก่อน

### D-64 ยังไม่รองรับไฟล์ .xlsx โดยตรง (รอบ 8)
รับเฉพาะ CSV เจ้าของร้านต้อง Save As เป็น CSV UTF-8 ก่อน
**เหตุผลที่ยังไม่ทำ** : ตัวอ่าน .xlsx ต้องพึ่งไลบรารีก้อนใหญ่ (SheetJS)
ซึ่งมีประวัติช่องโหว่และเพิ่มขนาด bundle มาก แลกกับความสะดวกหนึ่งขั้นตอน
**แนะนำ** : ถ้าเจ้าของร้านรำคาญจริง ค่อยแปลงฝั่งเซิร์ฟเวอร์ในงานเบื้องหลัง (รอบ 9)

### D-65 ⚠️ ไฟล์ที่บันทึกเป็น TIS-620 จะอ่านภาษาไทยเพี้ยน
ระบบอ่านไฟล์เป็น UTF-8 เสมอ ไฟล์เก่าที่ Excel ไทยบันทึกเป็น TIS-620
จะเห็นหัวคอลัมน์เป็นอักขระมั่ว แล้วจับคู่คอลัมน์ไม่ได้
**อาการที่เจ้าของร้านจะเจอ** : "หาคอลัมน์เลขพัสดุไม่เจอ" ทั้งที่ในไฟล์มี
**ยังไม่แก้เพราะ** : ต้องเดา encoding ซึ่งเดาผิดได้ และ CSV UTF-8 เป็นค่าเริ่มต้นของ Excel รุ่นใหม่แล้ว
**แนะนำ** : ถ้าเจอบ่อย ให้ตรวจจับ TIS-620 แล้วบอกวิธี Save As ที่ถูกต้อง

### D-66 การแจ้งลูกค้ายังต้องกดเอง ยังไม่มีตัวส่งเบื้องหลัง
กดหนึ่งครั้งส่งได้สูงสุด 60 ราย (ทยอย 10/นาที ≈ 6 นาทีต่อครั้ง)
ไฟล์ 300 ใบจึงต้องกด "ส่งต่อ" ประมาณ 5 ครั้ง
**เหตุผลที่ยังไม่ทำเป็นงานเบื้องหลัง** : ยังไม่มี worker ตัวจริง
และการให้คนกดเองทำให้เห็นผลทันทีและหยุดได้ทันทีถ้าผิดปกติ
**แนะนำ** : ทำพร้อม worker + quiet hours อัตโนมัติ (รอบ 9)

### D-67 🔴 บทเรียน — python .replace() ทำ OrderPatch รั่วอีกครั้ง
ตอนเพิ่มฟิลด์เลขพัสดุลง `OrderRow` สคริปต์แก้ไฟล์ไปแทนที่ทุกจุดที่ตรงกัน
ทำให้ฟิลด์ทั้งกลุ่มหลุดเข้าไปใน `OrderPatch` ด้วย
ซึ่งแปลว่า **เบราว์เซอร์จะทับเลขพัสดุและ `tracking_notified_at` ได้แบบไม่มีร่องรอย**
(ปลอม `tracking_notified_at` = ข้ามด่านกันแจ้งซ้ำได้ทันที)

ชุดทดสอบสถาปัตยกรรมที่เพิ่งเขียนเป็นตัวจับได้ แล้วชุดทดสอบ PostgreSQL
จับต่อว่า **TypeScript กันได้แค่ตอนคอมไพล์ ฐานข้อมูลยังรับ patch นั้นอยู่**
จึงย้ายด่านลงไปที่ `update_order` (ปักหมุดคอลัมน์ทั้งกลุ่มไว้กับค่าเดิม)

**บทเรียนซ้ำรอย D-5 (shipping_snapshot)** : สคริปต์แก้ไฟล์ต้องใส่ `assert`
และต้องระบุจำนวนครั้งที่จะแทนที่เสมอ — และกฎที่ "ผิดแล้วแก้ไม่ได้"
ต้องอยู่ในฐานข้อมูล ไม่ใช่ในชนิดข้อมูลของ TypeScript

### D-68 🔴 บทเรียน — เขียนทับฟังก์ชันโดยไม่ดูว่ามีคนแก้ไปแล้ว
ตอนแก้ `update_order` ใน 0011 ไปคัดลอกร่างจาก **0008** มาแก้
แต่ฟังก์ชันนี้ถูกเขียนทับไปแล้วทั้งใน **0009** (วิธีจัดส่ง + COD)
และ **0010** (`slip_media_id`) — ผลคือ migration 0011 ลบสองรอบนั้นทิ้งเงียบ ๆ
ชุดทดสอบวิธีจัดส่งจับได้ทันที (2 ข้อแดง)

**บทเรียน** : ก่อนเขียนทับฟังก์ชันของฐานข้อมูล ต้อง `grep` หา
`create or replace function <ชื่อ>` ทุกไฟล์ก่อนเสมอ แล้วใช้ร่างจากไฟล์ล่าสุด

### D-69 🔴 บทเรียน — ตัวจำลอง PostgREST แยก jsonb กับ array ไม่ออก
ตอนแรกแปลง array ของ JavaScript เป็นข้อความ JSON ทุกตัว
คอลัมน์ jsonb (`headers`, `problems`) ใช้ได้ แต่ `candidate_order_ids uuid[]` พัง
ถ้าไม่แปลงเลยก็สลับกันพังอีกด้าน

แก้โดยให้ตัวจำลองอ่านชนิดคอลัมน์จาก `information_schema` แล้วแปลงตามชนิดจริง
**บทเรียนซ้ำรอย D-50 / D-61** : ตัวจำลองที่ "เดา" คือแหล่งกำเนิดของเทสต์ที่ผ่านทั้งที่ของจริงพัง

### D-70 🔴 บทเรียน — สำเนาข้อมูลในคิว "เก่าได้" และนั่นคือจุดที่อันตรายที่สุด
ตอนเข้าคิวแจ้งลูกค้า ระบบถ่ายสำเนา `tracking_no` ไว้ใน `payload`
แล้วตอนส่งก็หยิบสำเนานั้นไปประกอบข้อความเลย

**อาการที่จะเกิดกับลูกค้าจริง** :
ไฟล์เดียวมีสองแถวชี้ออเดอร์เดียวกัน (ของสองกล่อง) แถวหลังทับเลขพัสดุบนออเดอร์
แต่คิวยังถือเลขของแถวแรก → **ลูกค้าได้เลขพัสดุของอีกกล่องหนึ่ง ซึ่งเป็นพัสดุที่มีอยู่จริง**
เกิดซ้ำได้อีกทางเมื่อแอดมินเห็นว่าเลขผิดแล้วแก้เองก่อนกดส่ง — ซึ่งคือทางแก้ที่ UI แนะนำเอง

แก้โดยให้ตัวส่ง **อ่านค่าจริงของออเดอร์ ณ ตอนจะส่งเสมอ** และข้ามงานถ้าเลขพัสดุหายไป
หรือออเดอร์ถูกยกเลิกไปแล้ว ส่วน `payload` เหลือไว้เป็นหลักฐานว่า "ตอนเข้าคิวตั้งใจจะส่งอะไร"
พร้อมเตือนที่หน้า preview เมื่อสองแถวชี้ออเดอร์เดียวกัน

**บทเรียน** : สำเนาใช้เป็น "หลักฐานของเจตนา" ได้ แต่ห้ามใช้เป็น "ความจริง ณ เวลาที่ลงมือ"

### D-71 🔴 บทเรียน — กรองสิทธิ์ "ก่อน" ตัดสินความกำกวม = เปลี่ยนความกำกวมเป็นความมั่นใจผิด ๆ
ตอนแรกกรองออเดอร์ตามเพจที่แอดมินเห็นได้ **ก่อน** ส่งเข้าตัวจับคู่

ผลคือ : ลูกค้าเบอร์เดียวมีออเดอร์สองใบคนละเพจ แอดมินที่เห็นเพจเดียว
จะเหลือผู้สมัครใบเดียว → ตัวจับคู่ตอบ `auto` ทั้งที่ความจริงคือกำกวม
→ เลขพัสดุของอีกเพจไปลงผิดใบ แล้วส่งข้อความหาลูกค้าผิดคน

แก้เป็น : จับคู่จากออเดอร์ **ทั้งหมด** ก่อน แล้วค่อยเอาสิทธิ์มาเป็นด่านทีหลัง
(ใบที่จับคู่ได้แต่อยู่นอกสิทธิ์ → ข้าม พร้อมบอกเหตุผล ไม่ใช่ไปหยิบใบอื่นมาแทน)
และเพิ่มเพดานพร้อมตัวจับ "ผลถูกตัด" เพราะการถูกตัดผลเงียบ ๆ ทำให้เกิดอาการเดียวกันเป๊ะ

**บทเรียน** : ความกำกวมต้องตัดสินจากความจริงทั้งหมดเสมอ
สิทธิ์เป็นด่านที่ "กันไม่ให้ทำ" ไม่ใช่ตัวกรองที่ทำให้ "มองไม่เห็นความจริง"

### D-72 ไม่มีด่านสิทธิ์ระดับรอบนำเข้า (แก้แล้ว)
เดิม `getImport` / `applyImport` / `cancelImport` / เส้นแจ้งลูกค้า ไม่ได้ตรวจสิทธิ์เลย
แอดมินคนไหนก็เปิดดูรายการรอบนำเข้า แล้วกด "ลงเลขพัสดุ" ของไฟล์ที่คนอื่นอัปโหลดได้ทั้งไฟล์
ซึ่งจะเขียนเลขพัสดุลงออเดอร์ของเพจที่ตัวเองไม่มีสิทธิ์เห็นด้วยซ้ำ

แก้ด้วยกฎที่อธิบายได้ตรงไปตรงมา : **เจ้าของร้านทำได้ทุกรอบ / แอดมินทำได้เฉพาะรอบของตัวเอง**
(รอบนำเข้าไม่ได้ผูกกับเพจใดเพจหนึ่ง ไฟล์เดียวอาจมีของหลายเพจ จึงใช้กฎนี้แทนสิทธิ์รายเพจ)

### D-73 งานแจ้งที่ค้างสถานะ "กำลังส่ง" (แก้แล้ว)
ถ้าโปรเซสตายระหว่างจองสิทธิ์กับจดผล แถวนั้นจะค้าง `claimed` ตลอดกาล
ไม่มีใครหยิบได้อีก และเจ้าของร้านไม่มีทางสั่งอะไรได้เลย

แก้ด้วย `expire_stale_notification_claims()` ที่ยกงานค้างเกิน 10 นาทีให้เป็น **"ไม่ทราบผล"**
⚠️ **ไม่ดึงกลับไปเป็น queued โดยเด็ดขาด** เพราะข้อความอาจยิงออกไปแล้วจริง ๆ ก่อนโปรเซสตาย
เจ้าของร้านจึงเห็นสถานะชัดเจน และมีทางออกผ่านเส้นทาง "ติ๊กยอมรับความเสี่ยง" ตามปกติ

### D-74 🔴 บทเรียน — เพดานงานต่อรอบต้องคำนวณจากเวลาที่คำขอมีให้
ตั้ง `MAX_PER_RUN = 60` โดยไม่ได้คูณกับจังหวะการส่ง
59 ช่อง × 6 วินาที = **354 วินาที** แต่ route ตั้ง `maxDuration = 300`
→ คำขอถูกตัดกลางทางทุกครั้งที่ส่งเต็มรอบ เจ้าของร้านเห็น "ติดต่อเซิร์ฟเวอร์ไม่ได้"
แทนสรุปผล และงานที่กำลังส่งจะค้างสถานะ (ไปเจอกับ D-73 พอดี)

แก้เป็น 40 ช่อง (234 วินาที) **และ** เพิ่มเพดานเวลาจริง (`RUN_BUDGET_MS`) เป็นตาข่ายชั้นสอง
เผื่อกรณี Meta ตอบช้ากว่าปกติ ครบเวลาแล้วหยุดแล้วคืน `remaining` ให้กดต่อ
มีชุดทดสอบคูณเลขให้ดูว่าเพดานยังพอดีกับ `maxDuration` อยู่

**ข้อจำกัดที่เหลือ** : จังหวะการส่งเป็น "ต่อคำขอ" ไม่ใช่ต่อเพจ
ถ้าเปิดสองแท็บแล้วกดพร้อมกัน อัตราส่งรวมจะเป็นสองเท่า (การจองสิทธิ์กันการส่งซ้ำได้
แต่ไม่ได้กันอัตรา) **แนะนำ** : ทำตัวคุมอัตราระดับเพจตอนมี worker จริง (รอบ 9)

### D-75 🔴 บทเรียน — เดาคอลัมน์แล้วต้องแก้ได้เสมอ
`guessMapping()` วนลูปผิดด้าน (เอาคอลัมน์เป็นวงนอก แทนที่จะเป็นคำใบ้)
ทำให้ลำดับความเฉพาะเจาะจงของคำใบ้ไม่มีผลเลย
หัวตาราง `['เบอร์ผู้ส่งของ','เบอร์ผู้รับของ']` จึงจับ **เบอร์ผู้ส่ง** (คือเบอร์ร้านเราเอง)

ที่แย่กว่าคือ **ไม่มีทางแก้เลย** เพราะหน้าเว็บไม่เคยแสดงการจับคู่คอลัมน์ให้ดู
และลายนิ้วมือไฟล์กันการอัปโหลดซ้ำไว้ → เจ้าของร้านติดตาย

แก้สามอย่างพร้อมกัน : กลับด้านลูป, เรียงคำใบ้ให้ "ผู้รับ" ชนะคำกว้าง,
และเก็บเนื้อไฟล์ไว้ (`tracking_imports.raw_csv`) เพื่อให้แก้การจับคู่แล้ว
**แกะไฟล์เดิมใหม่ได้ทั้งรอบ** โดยไม่ต้องหาไฟล์เดิมมาอัปโหลดซ้ำ
(เนื้อไฟล์ที่เก็บไว้ยังช่วยเรื่องการตรวจย้อนหลังตามข้อกำหนด G ด้วย)

**บทเรียน** : ฟีเจอร์ที่ "เดาให้" ต้องมาคู่กับทางแก้เสมอ
ไม่งั้นวันที่มันเดาผิด ผู้ใช้จะไม่มีทางไปต่อ

---

## รอบ 10 — แดชบอร์ด + คอมเมนต์ + แจ้งเตือน

### D-76 🔴 บทเรียน — ห้องแชทหนึ่งห้องต่อลูกค้าหนึ่งคน "ตลอดชีวิต"
ตาราง `conversations` มี unique index บน `customer_id`
= ลูกค้าคนเดิมกลับมาทักอีกใน 3 เดือน ก็ยังเป็นห้องเดิม ไม่ได้เปิดห้องใหม่

ตอนออกแบบกุญแจกันซ้ำของแจ้งเตือน เกือบใช้ `new_chat:<conversation_id>:<admin>:<channel>`
ซึ่งจะทำให้ **ลูกค้าเก่าที่กลับมาทักไม่มีใครได้รับแจ้งเตือนอีกเลยตลอดกาล**
เพราะเคยแจ้งไปแล้วครั้งหนึ่งเมื่อ 3 เดือนก่อน

นี่คือความพังที่ "เงียบ" ที่สุดเท่าที่เจอมาในโปรเจกต์นี้ — ไม่มี error ไม่มี log
มีแต่ลูกค้าที่ไม่มีใครตอบ และไม่มีใครรู้ว่าทำไม

แก้ด้วยกฎว่า `subject_id` ต้องมีของที่ "เปลี่ยนทุกครั้งที่เหตุการณ์เกิดใหม่" อยู่ด้วยเสมอ
(ข้อความ → `message_id` / เดินตรวจ → เวลาข้อความล่าสุด / คอมเมนต์ → `comment_id`)
มีทั้ง PG test และ architecture test คุมไว้แล้ว

**บทเรียน** : ก่อนใช้ id อะไรเป็นกุญแจกันซ้ำ ต้องถามก่อนว่า
"ของชิ้นนี้เกิดใหม่ได้ไหม หรืออยู่ยาวตลอดชีวิต"

### D-77 🔴 บทเรียน — กันซ้ำถาวร + ไม่ retry = ข้อมูลหายถาวร
`dedupe_key` มี unique index บล็อกการเข้าคิวซ้ำ **ตลอดกาล**
ตอนแรกเขียน `fail_notification` ให้ตีเป็น `failed` ทันทีที่ส่งพลาด

สองอย่างนี้รวมกันแปลว่า : **เน็ตสะดุด 10 วินาที = แจ้งเตือนนั้นหายตลอดกาล**
ตัวเดินตรวจรอบหน้าก็เข้าคิวใหม่ไม่ได้ เพราะกุญแจซ้ำ

แก้เป็นลองได้ 3 ครั้งแล้วค่อยยอมแพ้ (`claim_notifications` เป็นคนบวก `attempt_count`)

**บทเรียน** : "กันซ้ำถาวร" กับ "ไม่ลองใหม่" อยู่ด้วยกันไม่ได้
ถ้าจะกันซ้ำถาวร ต้องมีทางลองใหม่ ถ้าจะไม่ลองใหม่ ต้องยอมให้เข้าคิวซ้ำได้

### D-78 🔴 บทเรียน — การ "ตัดให้พอดี" ต้องรายงานว่าตัดอะไรไป
`buildBatchMessage()` ตัดรายการที่ล้นเพดาน 4096 ตัวอักษรของ Telegram ทิ้ง
แต่คืนมาแค่ข้อความ ไม่ได้บอกว่าใส่ไปได้กี่รายการ

ผู้เรียก (`flushNotifications`) จึงตีว่า "ส่งครบแล้ว" ทั้งที่งานที่ล้นออกไป
ถูกหยิบออกจากคิวไปแล้ว (claim ตีเป็น sent ไว้ก่อน) → หายตลอดกาลอีกเช่นกัน

แก้ด้วย `buildBatch()` ที่คืน `{ text, used }` แล้วให้ผู้เรียกคืนส่วนที่เหลือเข้าคิว
ด้วย `requeue_notification()` ซึ่ง **ลด `attempt_count` กลับด้วย**
ไม่งั้นวันที่งานเยอะจริง งานท้าย ๆ จะถูกนับครบ 3 ครั้งแล้วโดนทิ้งทั้งที่ยังไม่เคยถูกส่งเลย

**บทเรียน** : เดียวกับ D-71 — การถูกตัดผลเงียบ ๆ อันตรายกว่าการพังดัง ๆ เสมอ

### D-79 🔴 บทเรียน — "บอทตอบ" ไม่ใช่ "คนตอบ"
ตัวเดินตรวจแชทเงียบเช็คว่า "มีข้อความขาออกหลังข้อความล่าสุดของลูกค้าไหม"
ซึ่งนับ **ข้อความที่บอทตอบคีย์เวิร์ดไป** เป็นการตอบด้วย

ผลคือห้องที่บอททักไป "สวัสดีค่ะ" แล้วไม่มีคนมาต่อ จะไม่มีใครได้รับแจ้งเตือนเลย
ทั้งที่เป็นห้องที่ต้องเตือนที่สุด — ลูกค้าเข้าใจว่ามีคนคุยอยู่ แล้วรอนานกว่าเดิม

แก้เป็นเช็ค `sender_type = 'admin'` ไม่ใช่แค่ `direction = 'out'`

### D-80 ⭐ "เปิดอ่านแล้ว" ไม่เท่ากับ "ตอบแล้ว"
เกือบใช้ `is_read = false` เป็นเงื่อนไขของ `idle_15min` เพราะเขียนง่ายกว่ามาก
แต่แอดมินเปิดดูแล้ววางมือถือลง ลูกค้าก็ยังรออยู่ดี — แล้วห้องนั้นจะไม่มีใครเตือนอีกเลย

ใช้ "มีข้อความของแอดมินหลังข้อความล่าสุดของลูกค้าไหม" แทน มีเทสต์คุมไว้

### D-81 🔴 บทเรียน (ซ้ำรอย D-50 / D-61 / D-69) — อ่านฐานข้อมูลพลาดต้องดัง
`visiblePages()` เขียน `select('id,name,platform')` ทั้งที่คอลัมน์จริงชื่อ `page_name`
และไม่ได้ตรวจ `error` ที่ Supabase คืนมา → รายการเพจว่างเปล่าแบบเงียบสนิท
ผลคือทุกคนตั้งค่าเพจในหน้าแจ้งเตือนไม่ได้ โดยไม่มีอะไรบอกว่าทำไม

PG test เป็นคนจับได้ แก้ชื่อคอลัมน์ + โยน error ทุกครั้งที่อ่านไม่สำเร็จ

**บทเรียน** : ทุก `await db().from(...)` ที่ไม่ได้เช็ค `error` คือบั๊กที่รอเกิด

### D-82 `drainWebhookQueue` ลืมบวกฟิลด์ใหม่ (แก้แล้ว — บั๊กตกค้างจากรอบ 9)
รอบ 9 เพิ่ม `comments_saved` / `comments_flagged` ลง `ProcessSummary`
แต่ลืมมาบวกใน `drainWebhookQueue` ที่ไล่บวกทีละฟิลด์

ผลคือ `npm run worker` และชุดทดสอบรายงาน "บันทึกคอมเมนต์ 0 รายการ" ทั้งที่บันทึกได้จริง
TypeScript จับไม่ได้เพราะเป็นการ **ไม่เขียน** ไม่ใช่การเขียนผิดชนิด

**สิ่งที่ยังค้าง** : ควรเปลี่ยนเป็นวนรวมทุกคีย์อัตโนมัติ (`Object.keys`) ในรอบเก็บกวาด
จะได้ไม่ต้องพึ่งความจำอีก — ยังไม่ทำในรอบนี้เพราะจะกระทบชุดทดสอบหลายไฟล์

### D-83 `dispatchNotification` อ่านตารางแอดมินใหม่ทุกข้อความ
ทุกข้อความที่ลูกค้าส่งเข้ามา = อ่าน `admins` + `notification_prefs` อีกสองครั้ง
ที่ 3–5 แอดมินยังไม่เป็นปัญหา แต่ถ้าวันหนึ่งมีแอดมิน 50 คนหรือข้อความเข้าถี่มาก จะเริ่มหนัก

**ตั้งใจยังไม่ทำแคช** เพราะแคชค่าตั้งแจ้งเตือนแปลว่า "ปิดแจ้งเตือนแล้วยังเด้งอยู่อีก 30 วินาที"
ซึ่งอธิบายให้ผู้ใช้เข้าใจยาก ถ้าจะทำ ต้องมีทางล้างแคชทันทีตอนกดบันทึกค่าตั้ง

### D-84 ตัวเดินตรวจต้องมีคนตั้งให้ทำงาน — ไม่ได้ทำงานเอง
`idle_15min` และ `window_closing` ไม่มี webhook ยิงมาบอก
ต้องรัน `npm run notify` หรือตั้ง cron ยิง `POST /api/notify/flush` ทุก 5 นาที

**ถ้าไม่ตั้ง ระบบยังทำงานปกติทุกอย่าง แต่จะได้แจ้งเตือนแค่ 3 ใน 5 เหตุการณ์**
และจะไม่มีอะไรเตือนว่าขาดไป — เขียนไว้ใน `docs/NOTIFICATIONS.md` หัวข้อ 5 แล้ว
**ควรทำในรอบเก็บกวาด** : ขึ้นป้ายเตือนในหน้าตั้งค่าถ้าไม่มีการ flush มาเกิน 30 นาที

### D-85 เพดานตรวจ 200 ห้องต่อรอบ
`scanIdleAndClosing()` ตรวจสูงสุด 200 ห้องต่อรอบ เรียงจากห้องที่ลูกค้ารอนานสุดก่อน
ถ้าวันไหนมีเกิน 200 ห้องค้างพร้อมกัน ห้องที่เหลือจะได้แจ้งเตือนในรอบถัดไป (ช้าไปอีก 1 นาที)
ยอมรับได้ที่ขนาดร้าน 3–5 แอดมิน แต่ถ้าโตกว่านี้ต้องทำเป็น cursor เดินต่อจากที่ค้าง

### D-86 แจ้งเตือนที่ยิงออกไปแล้วแต่ไม่ทราบผล
`claim_notifications` ตีสถานะเป็น `sent` ตั้งแต่ตอนหยิบ (จองสิทธิ์)
ถ้าโปรเซสตายระหว่างหยิบกับส่งจริง งานนั้นจะค้างสถานะ `sent` ทั้งที่ไม่มีใครได้รับ

**ตั้งใจเลือกทางนี้** : แจ้งเตือนซ้ำน่ารำคาญกว่าแจ้งเตือนหาย และการกู้คืนอัตโนมัติ
มีความเสี่ยงยิงซ้ำ ต่างจากกรณีข้อความหาลูกค้า (D-73) ที่ต้องมีสถานะ "ไม่ทราบผล" ชัดเจน
เพราะแจ้งเตือนไม่ได้ไปถึงลูกค้า จึงไม่ต้องมีร่องรอยตรวจสอบระดับเดียวกัน

### D-41 (ย้ำ) หมุนความลับทั้งหมดก่อนใช้งานจริง
รอบนี้เพิ่มความลับใหม่เข้ามาอีกสามตัว ต้องอยู่ในรายการหมุนด้วย :
`VAPID_PRIVATE_KEY` · `TELEGRAM_BOT_TOKEN` · `CRON_SECRET`

⚠️ **หมุน `VAPID_PRIVATE_KEY` แล้วทุกคนต้องกดอนุญาตแจ้งเตือนใหม่หมด**
จึงควรหมุน "ครั้งเดียวตอนขึ้นใช้จริง" แล้วไม่หมุนอีก เว้นแต่รู้ว่าหลุดจริง

### D-87 🔴 บทเรียน — ชื่อคอลัมน์เป็น "สตริง" ไม่มีใครตรวจให้
หน้าสรุปยอดขอคอลัมน์ `customers.referral_ad_id` ซึ่ง **ไม่เคยมีอยู่จริง**
ในไฟล์ migration ไหนเลยตั้งแต่ 0001 ถึง 0013

ที่น่ากลัวคือ **ไม่มีด่านไหนจับได้เลยสักด่าน** :

| ด่าน | ทำไมไม่จับ |
|---|---|
| TypeScript | ชื่อคอลัมน์เป็นสตริง `.select('...')` ไม่ใช่ชนิดข้อมูล |
| ชุดทดสอบเดิม | ทดสอบแต่ `metrics.ts` ซึ่งเป็นฟังก์ชันบริสุทธิ์ที่รับข้อมูลที่ป้อนให้ |
| ฐานข้อมูลปลอม | ไม่มี schema จริงให้ชน |
| `next build` | ไม่ได้ต่อฐานข้อมูล |
| architecture test | ตรวจ "โค้ดอยู่ถูกที่ไหม" ไม่ได้ตรวจ "คอลัมน์มีจริงไหม" |

→ ไปพังเอาบนเครื่องผู้ใช้ตอนเปิดหน้า เป็น error 500 เต็ม ๆ

**ที่มาของแอดในระบบนี้ (schema จริง)** :
```
webhook (ref / ad_id ติดมากับข้อความแรก)
  → conversations.referral_ad_id     ← เก็บที่นี่ที่เดียว (0005)
    → orders.referral_ad_id           ← คัดลอกตอนสร้างออเดอร์ (0008 / 0009)
```
ตาราง `customers` ไม่เคยเก็บ และ **ไม่ควรเก็บ** เพราะลูกค้าคือ "คน"
ส่วนแอดเป็นของ "การทักครั้งนั้น" ซึ่งคือห้องแชท
เพิ่มคอลัมน์ซ้ำที่ `customers` = มีความจริงสองแหล่งที่ขัดกันเองได้ (บทเรียนเดียวกับ D-5)

**จึงไม่มี migration 0014** — แก้ที่คำสั่งอ่าน ไม่ใช่ที่ฐานข้อมูล

**สิ่งที่เพิ่มเข้ามาเป็นด่านถาวร** :
1. คอลัมน์ทุกชุดที่หน้าสรุปยอดขอ ถูกประกาศเป็นค่าคงที่และส่งออกเป็น `DASHBOARD_SELECTS`
2. `tests/pg/dashboard.pg.test.ts` เดินไล่ทุกชื่อในนั้นเทียบ `information_schema`
   บนฐานข้อมูลที่รัน migration ครบทุกไฟล์ตั้งแต่ 0001
3. มีเทสต์ที่ยืนยันว่า `customers` **ต้องไม่มี** `referral_ad_id`
   (ถ้าวันหนึ่งมีคนเผลอเพิ่ม เทสต์จะแดงเพื่อบังคับให้คุยกันก่อน)
4. เลิกใช้ `as unknown as` ตรงจุดที่แปลงผลลัพธ์ เปลี่ยนเป็นตัวบอกชนิด (type predicate)
   — การแคสต์ทับสองชั้นคือสิ่งที่ทำให้ TypeScript เงียบสนิทตั้งแต่แรก

**สิ่งที่ยังค้าง** : ด่านนี้ครอบคลุมเฉพาะหน้าสรุปยอด
โมดูลอื่น (inbox / orders / comments) ยังใช้สตริงสดอยู่ ควรทยอยย้ายมาใช้รูปแบบเดียวกัน
ตอนนี้ยังไม่พบว่าผิด เพราะมี PG test ที่เรียกใช้จริงคุมอยู่แล้วทุกโมดูล
แต่ "เรียกใช้จริง" กับ "ไล่ตรวจทุกคอลัมน์" ไม่เท่ากัน — เส้นทางที่ไม่ค่อยถูกเรียกยังเสี่ยง

### D-88 `as unknown as` คือจุดบอดที่ต้องไล่เก็บ
`as unknown as T` ปิดปาก TypeScript ทั้งหมด ไม่เหลือการตรวจอะไรเลย
D-87 หลุดมาได้เพราะบรรทัดนี้ตรง ๆ

ในโค้ดยังมีที่ใช้รูปแบบนี้อยู่อีกหลายจุด (ส่วนใหญ่ตอนแปลงผลจาก `.rpc()`
ซึ่งไม่มีชนิดข้อมูลให้ตั้งแต่ต้น จึงพอมีเหตุผล)
**ควรทำในรอบเก็บกวาด** : ไล่ดูทีละจุดว่าจุดไหน "แคสต์เพราะจำเป็น"
กับจุดไหน "แคสต์เพราะขี้เกียจ" แล้วเปลี่ยนกลุ่มหลังเป็นตัวบอกชนิดจริง

---

## ก้อน BUGFIX-1 — identity / header / reply

### D-89 ✅ แก้แล้ว — ป้ายนโยบายยาวจนดันเลย์เอาต์หัวห้องพัง
`summariseForAdmin()` คืน `label_th` เป็นประโยคเต็มตอนส่งไม่ได้ เช่น
"ตอนนี้ยังส่งข้อความหาลูกค้ารายนี้ไม่ได้ตามกฎของ Meta ..."
แล้วหัวห้องเอาไปใส่ `<Badge>` ในแถวเดียวกับรูป/ชื่อ/ปุ่ม → เบียดจนใช้บนมือถือไม่ได้

**แก้ที่การแสดงผลล้วน ๆ** เพิ่ม `badge_th` (สั้น) + `detail_th` (เต็ม)
โดย **ไม่แตะ `can_send` / `reason_th` / การตัดสินของ Policy Engine เลย**
เหตุผลเต็มย้ายไปอยู่ใต้ช่องพิมพ์ ซึ่งมีที่ให้อ่านจริง และเป็นจังหวะที่แอดมินต้องการพอดี
มีเทสต์บังคับว่าป้ายต้องยาวไม่เกิน 30 ตัวอักษรทั้งกรณีส่งได้และส่งไม่ได้

### D-90 ✅ แก้แล้ว — ตรรกะชื่อลูกค้ากระจายอยู่หลายที่
`c.customer_name || 'ลูกค้า xxxxxx'` ถูกเขียนซ้ำในลิสต์แชทและหัวห้อง
เวลาปรับจะลืมแก้บางจุด แล้วสองที่จะแสดงคนละชื่อ ซึ่งทำให้แอดมินสับสนว่าคุยกับใครอยู่

รวมไว้ที่ `src/lib/customer-name.ts` ที่เดียว และเพิ่มกฎที่ขาดไป :
ชื่อที่เป็นช่องว่างล้วนต้องนับว่า "ไม่มีชื่อ" (เดิมจะแสดงเป็นช่องว่างเปล่า)

### D-91 ✅ แก้แล้ว — ไม่มีรูปโปรไฟล์ในลิสต์และหัวห้อง
เพิ่ม `CustomerAvatar` ที่ขนาดคงที่เสมอ (ไม่งั้นลิสต์กระตุกตอนรูปโหลดเสร็จ)
และตกไปใช้ตัวอักษรย่อเมื่อรูปโหลดไม่ขึ้น — **ลิงก์รูปของ Meta หมดอายุได้ตลอดเวลา**

⚠️ ตัวอักษรย่อต้องใช้ `[...str]` ไม่ใช่ `str[0]`
เพราะสระ/วรรณยุกต์ไทยเป็นอักขระแยก การตัดด้วย index จะได้สระลอย ๆ มาแทน

### D-92 STILL OPEN — CODE : ยังไม่มีปุ่ม "ดึงชื่อทั้งหมด" ระดับเพจ
ตอนนี้ปุ่มดึงชื่อทำได้ทีละห้อง
ถ้าเจ้าของร้านเพิ่งแก้สิทธิ์ Meta แล้วมีลูกค้าค้างอยู่หลายร้อยคน
ต้องรอให้ระบบทยอยดึงเองตามจังหวะ (ซึ่งจะได้ครบภายในไม่กี่วัน) หรือกดทีละห้อง

**ควรทำ** : ปุ่มระดับเพจใน ตั้งค่า → จัดการเพจ ที่สั่ง `reset_profile_sync` ทั้งเพจ
แล้วให้ worker ทยอยดึง — ยังไม่ทำในก้อนนี้เพราะต้องมีตัวคุมอัตราไม่ให้ยิง Meta รัว

---

## ก้อน BUGFIX-2a — ตอบกลับข้อความ + เมนูข้อความ

### D-93 ✅ แก้แล้ว — "ยกมาอ้างอิง" ไม่ใช่การตอบกลับจริง
เดิมกดแล้วเอาข้อความไปใส่ `>` นำหน้าในช่องพิมพ์
ผลคือข้อความที่ยกมากลายเป็น **เนื้อข้อความที่ลูกค้าได้รับจริง**
ลูกค้าจึงเห็นข้อความตัวเองซ้ำอีกรอบแบบมี `>` นำหน้า ซึ่งอ่านแล้วงง

ตอนนี้เก็บเป็น **ความสัมพันธ์** (`messages.reply_to_message_id`) แยกจากเนื้อข้อความ
มี preview เหนือช่องพิมพ์ + ยกเลิกได้ + ฟองข้อความแสดงข้อความต้นทาง

### D-94 🔴 บทเรียน — Messenger กับ Instagram ไม่เท่ากัน (ยืนยันกับเอกสารแล้ว)
ตรวจเอกสาร Meta ก่อนเขียนโค้ด (ไม่เดา payload ตามกฎของโปรเจกต์) :

| ช่องทาง | `reply_to.mid` | ที่มา |
|---|---|---|
| Messenger | ✅ ระบุไว้ชัดเจน | Send API reference |
| Instagram | ❌ **ไม่ได้ระบุไว้เลย** | Send Messages (IG API with IG Login) |

จึงทำเป็น `native_reply` ใน **policy config** (ที่เดียวกับกฎ Meta อื่น ๆ)
โดย Instagram ตั้งต้นเป็น `false` = fail-closed เหมือน transport ที่ยังไม่ verified

**ทำไมไม่ยัดไปเลยเผื่อฟลุก** :
ถ้า Meta เงียบ ๆ ไม่สนใจฟิลด์นั้น เราจะบันทึกว่า "ตอบกลับแล้ว"
ทั้งที่ลูกค้าไม่เห็นเส้นโยงอะไรเลย = **โกหกตัวเองในประวัติข้อความ**
จึงแยกเก็บ `reply_native` ไว้ต่างหาก เพื่อให้รู้ย้อนหลังได้ว่าอันไหน native จริง

⚠️ **ถ้าวันหนึ่ง Meta เพิ่ม reply_to ให้ IG** → เปิดด้วย
`POLICY_INSTAGRAM_NATIVE_REPLY=true` ได้ทันที ไม่ต้องแก้โค้ด

### D-95 ✅ ด่านกันปลอม — ตอบกลับข้ามห้องแชท
หน้าเว็บส่งมาได้แค่ **uuid ของข้อความในระบบเรา** ห้ามส่ง mid ของ Meta มาเอง
(ไม่งั้นยัด mid ของห้องอื่น/เพจอื่นมาแปะได้ = ข้อมูลรั่วข้ามลูกค้า)

กันไว้ **3 ชั้น** :
1. `resolve_reply_target()` ตรวจว่าอยู่ห้องเดียวกันก่อนแปลงเป็น mid
2. trigger `messages_reply_same_conversation` กันตอน insert (บทเรียน D-68 —
   TypeScript อย่างเดียวไม่ใช่หลักประกัน ต้องให้ฐานข้อมูลกันชั้นสุดท้าย)
3. `sendMessage()` ทับ `reply_to_meta_mid` ที่ผู้เรียกยัดมาเสมอ

⭐ ตอบกลับข้ามห้อง = **ปฏิเสธทั้งคำขอ** ไม่ใช่ "ส่งไปโดยไม่ตอบกลับ"
เพราะแอดมินตั้งใจอ้างอิงข้อความหนึ่ง ถ้าเงียบ ๆ ส่งไป ข้อความจะถึงลูกค้า
โดยขาดบริบทที่ตั้งใจไว้ ซึ่งอาจทำให้เข้าใจผิด

### D-96 ⚠️ ข้อจำกัดที่ยอมรับ — HUMAN_AGENT / UTILITY / MARKETING ยังไม่รองรับ reply
ใส่ `reply_to` ไว้เฉพาะ adapter `STANDARD` เท่านั้น
เพราะอีกสาม transport ยัง `enabled = false` ทั้งหมด (D-1/D-2/D-3) จึงยังไม่มีทางถูกเรียก
**ถ้าวันหนึ่งเปิด transport เหล่านั้น** ต้องกลับมาใส่ `withReplyTo()` ใน adapter นั้นด้วย
ไม่งั้นการตอบกลับจะเงียบหายไปเฉพาะบางช่องทางแบบหาสาเหตุยาก

### D-97 ✅ ปิดแล้ว — ก้อน 2 ครบทุกข้อ (1.3–1.12)
ทำครบใน BUGFIX-2a (1.3/1.4) และ 2b (1.5–1.12)
บันทึกเดิมเก็บไว้ด้านล่างเป็นประวัติ

<details><summary>บันทึกเดิม</summary>

### D-97 (เดิม) — ก้อน 2 ยังเหลือ 1.5–1.12
ทำเสร็จในก้อนนี้ : **1.3 (ตอบกลับจริง)** และ **1.4 (เมนูข้อความ)**

ยังไม่ได้ทำ : 1.5 extract → workflow · 1.6 customer workspace ·
1.7 create order from chat · 1.8 shipping quick action · 1.9 canned variables ·
1.10 product quick insert · 1.11 order history ในห้อง · 1.12 slip/image workflow

**เหตุผลที่แยก** : 1.3 แตะสายส่งข้อความและ Policy Engine ซึ่งเป็นส่วนที่พังแล้วเสียหายที่สุด
จึงทำให้จบพร้อมเทสต์ครบก่อน แล้วค่อยทำงาน UI ที่เหลือซึ่งไม่แตะสายส่ง

</details>

---

## ก้อน BUGFIX-2b — พื้นที่ทำงานลูกค้า + ปุ่มลัด (1.5–1.12)

### D-98 🔴 บทเรียน — D-87 เกือบซ้ำรอยในไฟล์ที่เพิ่งเขียนเอง
ตอนเขียน `quick-actions.ts` เผลอเขียน `products.variants` / `products.base_price`
ซึ่ง **ไม่มีอยู่จริง** (คอลัมน์จริงคือ `variant` / `price`)

TypeScript จับไม่ได้เพราะเป็นสตริงเหมือนเดิมเป๊ะ — ห่างจาก D-87 แค่ไม่กี่ชั่วโมง

**นี่คือหลักฐานว่าบทเรียนอย่างเดียวไม่พอ ต้องมีด่านอัตโนมัติ**
จึงประกาศคอลัมน์เป็นค่าคงที่ + ส่งออกเป็น `QUICK_ACTION_SELECTS`
แล้วมี PG test ไล่ตรวจกับ `information_schema` เหมือนที่ทำกับ dashboard

⭐ **แนวทางนี้ควรขยายไปทุกโมดูลในก้อน 3** (inbox / orders / comments / notifications)

### D-99 ✅ ค่าที่เป็น "ความจริงของร้าน" ถูกล็อกไว้ฝั่งเซิร์ฟเวอร์
ราคา / ยอดรวม / เลขพัสดุ / ชื่อลูกค้า — เบราว์เซอร์ส่งมาเองไม่ได้เลย
ส่งได้แค่ "จะประกอบอะไร" (`kind`) กับ `product_ids` / `template` / `order_id`

มี architecture test คุมสามชั้น :
1. schema ของ compose route ห้ามมีช่องรับ `price` / `total` / `tracking_no` / `carrier`
2. ตัวเลือกสินค้าฝั่งเบราว์เซอร์ต้องส่งไปแค่ id
3. compose route ห้ามเรียก `sendMessage` เลย (วางในช่องพิมพ์เท่านั้น)

### D-100 🔴 ข้อความที่ยังไม่สมบูรณ์ ต้องไม่หลุดไปถึงลูกค้า
ตัวแปรที่ไม่มีค่า **คง `{{...}}` ไว้** ไม่แทนด้วยช่องว่าง

เหตุผล : ถ้าแทนด้วยช่องว่าง ข้อความจะกลายเป็น "เลขพัสดุคือ  ค่ะ"
ซึ่ง **ดูปกติพอที่คนรีบจะกดส่ง** — การคงโค้ดไว้ทำให้ผิดจนมองข้ามไม่ได้
ประกอบกับ `missing` ที่คืนไป หน้าเว็บขึ้นคำเตือนได้อีกชั้น

เช่นเดียวกับ "ข้อมูลจัดส่ง" ที่ขาดที่อยู่ → บอกว่าขาด ไม่ประกอบข้อความครึ่ง ๆ กลาง ๆ
(ลูกค้ายืนยันที่อยู่ที่ไม่มีอยู่จริง = พัสดุไปผิดที่ = แก้ทีหลังไม่ได้)

### D-101 ✅ ออเดอร์ที่ยกเลิกแล้ว ห้ามเป็น "ออเดอร์ล่าสุด"
ถ้าไม่กรอง แอดมินจะกด "ข้อมูลจัดส่ง" แล้วได้เลขพัสดุของออเดอร์ที่ถูกยกเลิกไปให้ลูกค้า
กรองด้วย `.neq('status','cancelled')` + มี PG test ยืนยัน

### D-102 ✅ ด่านสิทธิ์มีชุดเดียว
`requireConversationAccess` เดิมเป็น private ในโมดูล inbox
ตอนนี้ export ออกมาให้ `customers/workspace` และ `chat/quick-actions` ใช้ตัวเดียวกัน

⚠️ ตั้งใจ **ไม่** เขียนด่านสิทธิ์ขึ้นใหม่ในโมดูลใหม่
ด่านที่มีสองชุด คือด่านที่จะไม่ตรงกันในวันที่มีคนแก้ชุดเดียว

### D-103 STILL OPEN — CODE : ยังไม่ได้ทำในก้อน 2
สิ่งที่ทำไม่ครบตามบรีฟข้อ 1.7 / 1.12 :

**1.7** — `OrderDialog` เดิมยังใช้ได้ตามปกติ และมี "ใส่สรุปออเดอร์ในช่องพิมพ์" แล้ว
แต่ยัง **ไม่ได้ preload ข้อมูลที่ extract มา** ลงฟอร์มสร้างออเดอร์อัตโนมัติ
(แอดมินยังต้องกรอกเอง หรือกดบันทึกข้อมูลลูกค้าก่อนแล้วค่อยสร้างออเดอร์)

**1.12** — ยังไม่มีเมนู "แนบรูปนี้เข้าออเดอร์ / ตั้งเป็นสลิป" ตอนกดที่รูป
ปัจจุบันรูปเปิดดูได้และใช้สำเนาถาวรของเราแล้ว (D-17) แต่ผูกเข้าออเดอร์จากในแชทยังไม่ได้
ต้องไปที่หน้าออเดอร์แล้วอัปโหลดสลิปที่นั่นแทน

**เหตุผลที่ยังไม่ทำ** : ทั้งสองข้อต้องแก้ `OrderDialog` กับสาย media ซึ่งเป็นโค้ดที่มีอยู่แล้ว
และมีเส้นทางข้อมูลของตัวเอง — แก้ครึ่ง ๆ เสี่ยงทำของที่ใช้อยู่พัง
**ควรทำต่อในก้อน 3** พร้อมการตรวจสาย media ตามบรีฟข้อ 6
